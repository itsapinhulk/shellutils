#!/usr/bin/env bash
# Wrapper that forwards all arguments to `py/broad-permission-claude.py`.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/../py/broad-permission-claude.py" "$@"
