#!/usr/bin/env bash
# Wrapper that forwards all arguments to `py/view-json.py`.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/../py/view-json.py" "$@"
