#!/usr/bin/env bash
# Wrapper that forwards all arguments to `py/find-unexpected-dirs.py`.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/../py/find-unexpected-dirs.py" "$@"
