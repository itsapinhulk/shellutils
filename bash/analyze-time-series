#!/usr/bin/env bash
# Wrapper that forwards all arguments to `py/analyze-time-series.py`.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/../py/analyze-time-series.py" "$@"
