[project]
name = "shellutils"
version = "1.0"
description = "A collection of shell utilities"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]
